import fs from "fs";
import path from "path";
import U_createFolder from "../../utils/U_createFolder";

describe("Test U_createFolder()", () => {
  it("Should create folder", () => {
    const id = "test-folder-name";
  });
});
